inverse-iv-surface-pinn/
  notebooks/
    data_wrds.ipynb  # 连 WRDS 做清洗与特征，输出 data/processed/*.parquet
    mvp_smile_fit.ipynb  # 先做单期限微笑的最小可行实验（验证优化/绘图链路）
    surface_fit_adam_lbfgs.ipynb  # 全曲面训练与评估（Adam→L-BFGS），生成主要结果图
    pinn_constraints.ipynb  # 加入/消融 PINN 风格约束（PDE 残差等）
    figures.ipynb  # 只读 out/ 的结果，出高分辨率图用于报告/幻灯片
  ivpinn/
    __init__.py
    data.py          # WRDS/OptionMetrics加载与清洗、mid价等
    bs.py            # Black–Scholes定价/Greeks
    surface.py       # σ(K,T) 参数化：插值/样条/MLP
    losses.py        # 定价误差, 平滑, 无套利, (可选) PDE残差
    train.py         # 训练/评估工具函数（Adam→L-BFGS）
    eval.py          # 误差分解、曲面/截面/残差图
  configs/
    default.yaml
    pinn_ablation.yaml
  scripts/
    wrds_fetch.py    # （可选）命令行拉数脚本
  tests/
    test_bs.py
  data/              # 原始/中间数据（gitignore）
  out/               # 结果图表/模型(checkpoint)
  requirements.txt
  .gitignore
  README.md
  LICENSE
