本项目的目标是基于 WRDS 的 OptionMetrics 数据，将从欧式期权市场价格反演隐含波动率曲面 
𝜎
(
𝐾
,
𝑇
)
σ(K,T) 视为一个病态的 Black–Scholes 逆问题，在神经网络与物理约束（PINN）框架下，构建一个平滑、无套利且可复现的隐含波动率估计方法。具体来说：

数据层面
利用 WRDS 提供的 SPY 期权与相关基准数据（期权链、标的价格、零息收益率曲线、股息/分红信息等），构建一个结构化的数据集，对不同行权价 
𝐾
K 和到期时间 
𝑇
T 的期权中间价进行清洗和预处理，作为逆问题的观测输入。

模型层面
采用多层感知机（MLP）对隐含波动率曲面 
𝜎
(
𝐾
,
𝑇
)
σ(K,T) 进行参数化，将 
(
𝐾
,
𝑇
)
(K,T)（或经适当归一化的 moneyness 与到期）映射到对应的波动率，并通过 Black–Scholes 闭式定价公式，将模型输出的 
𝜎
σ 转换为期权理论价格，与市场价格进行对比，从而间接完成对 
𝜎
(
𝐾
,
𝑇
)
σ(K,T) 的反演。

正则化与 PINN 约束
在损失函数中同时引入

价格拟合误差（模型定价与市场中间价的偏差），

曲面平滑正则（抑制在 
𝐾
K 与 
𝑇
T 方向上的剧烈振荡），

静态无套利约束（总方差随期限单调不减、价格对行权价单调递减且凸），
必要时结合基于 Black–Scholes / Dupire 方程的 PINN 风格残差项，将金融理论以“软约束”的方式融入训练过程，使得到的 
𝜎
(
𝐾
,
𝑇
)
σ(K,T) 在经济上合理且近似无套利。

实验评估与泛化分析
在一个或多个交易日的 SPY 期权数据上，对所得到的隐含波动率曲面进行系统评估：

定量比较模型定价误差与原始市场隐含波动率散点的拟合效果；

检查并量化无套利条件的违背程度；

分析在不同市场状态（不同日期）下方法的稳定性和泛化能力。

通过以上目标，本项目希望给出一个基于 WRDS 实证数据、结合深度学习与物理约束的隐含波动率曲面反演方案，并展示在真实市场环境中，如何在“拟合精度”和“无套利、平滑性”之间取得可解释的折中。